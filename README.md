# NodeJSReactApp
Web app created with React as front-end and NodeJS as back-end.

# Preparation
In `client` and `server` directory run
```
npm ci
```
# Build
In `client` run
```
npm run build
```

# Run
In `server` run
```
npm run start
```
